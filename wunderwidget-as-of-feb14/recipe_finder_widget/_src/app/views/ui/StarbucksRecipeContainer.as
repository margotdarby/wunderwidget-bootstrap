package app.views.ui{import app.utils.SelectionSystem;import app.interfaces.SelectionSystemItem;import flash.net.*;import flash.events.MouseEvent;//import app.utils.GoogleTracker;import app.models.RecipeModel;import app.events.ModelEvent;public class StarbucksRecipeContainer extends SelectionSystem {	//-------------------------------------------------------------// PRIVATE VARIABLES//-------------------------------------------------------------	private var _model:RecipeModel;	private var _rb:StarbucksRecipeButton;	//-------------------------------------------------------------// CONSTRUCTOR//-------------------------------------------------------------			public function StarbucksRecipeContainer(model:RecipeModel) {		_model = model;			}//-------------------------------------------------------------// PRIVATE METHODS	//-------------------------------------------------------------	private function attachRecipes():void {		for (var i:int = 0; i < _model.recipesLength; i++) {			_rb = new StarbucksRecipeButton(_model.recipeList[i].rname, _model.recipeList[i].id, _model.recipeList[i].link);			_rb.y = i*(_rb.height + 1);			addChild(_rb);		}	}//-------------------------------------------------------------// EVENT HANDLERS//-------------------------------------------------------------	override protected function clickHandler(evt:MouseEvent):void {		if (evt.target is SelectionSystemItem) {			//August 14: stripping out EW			//EW.clickTags["starbucks recipes"] = evt.target.url;			//EW.clickthru("starbucks recipes", "starbucks recipe click-" + evt.target.buttonLabel);			//navigateToURL(new URLRequest(evt.target.url));		}	}	//-------------------------------------------------------------// GETTER / SETTERS//-------------------------------------------------------------	//-------------------------------------------------------------// PUBLIC METHODS//-------------------------------------------------------------	public function showRecipes():void {		if(this.numChildren == 0) {			attachRecipes();		}else{					}	}//-------------------------------------------------------------// END OF CLASS	//-------------------------------------------------------------}}