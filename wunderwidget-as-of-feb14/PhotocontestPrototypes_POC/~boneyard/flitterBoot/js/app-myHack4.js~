//Cute little function copied from flickrBomb: allows you to enter search keyword(s) within live, editable text. It doesn't do anything that a standard keyword-submit form doesn't do, but it

/*(function(){
    var fstrope = flitter();
    
    var currentValue = false,
    el = $('#demo-content span');
    el.focus(function(){
        currentValue = $(this).text();
    });
    el.keypress(function (e) {
        if (e.which == 13) {
            e.preventDefault();
            updateIMG($(this));
        }
    });
    el.focusout(function() {
        updateIMG($(this));
    });
    function updateIMG(argu) {
        if (argu.text() !== currentValue) {
            var keywords = $('#keywords').text();
            $('#demo-splash').html('<img src="kw:' + keywords + '" width="800px" height="250px" style="display:none"><span>Loading New Demo Content</span>');
            fstrope.bomb();
        } else {
            return false;
        }
    }
})();
*/